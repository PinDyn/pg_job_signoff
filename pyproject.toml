[project]
name = "pg_job_signoff"
authors = [
    { name = "PinDyn", email = "marcel@pindyn.com"}
]
description = "PG Aluminium Job Completion on-site sign-off links (technician + client)"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
